version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /web
    ports:
      - "8081:5000"
    command:
      - sh
      - -c
      - |
        gunicorn -w 1 -k gevent -t 30 -b :5000 app:app
    restart: always


    networks:
      docker-network:
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "1"

networks:
  docker-network:
    driver: bridge

volumes:
  docker-volume:
